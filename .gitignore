# Logs (root runtime log dir only — do not use bare `logs` or src/**/logs is ignored)
/logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# Do not commit a root symlink named like the repo (WSL paths break @electron/packager on Windows).
/agent-cosmo-hub

dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
